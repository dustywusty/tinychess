export default function App() {
  return (
    <main className="min-h-screen flex items-center justify-center bg-bg text-text">
      <div className="max-w-md text-center space-y-3 p-6">
        <h1 className="text-2xl font-semibold">Tiny Chess</h1>
        <p className="opacity-80">
          The React rewrite is in progress. The current playable client lives at
          the existing routes served from <code>internal/templates</code>.
        </p>
      </div>
    </main>
  );
}
