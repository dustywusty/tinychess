# Replace the version with a published release tag, or use its immutable digest.
TINYCHESS_IMAGE=ghcr.io/dustywusty/tinychess:0.1.0
DOMAIN=chess.example.com
# Optional: game history only. Active games do not survive server restarts.
DATABASE_URL=
